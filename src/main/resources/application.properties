spring.application.name=backend-sigopel-v1
server.port=9090
# Logging de Spring Security
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web=TRACE
logging.level.com.example.backend_sigopel_v1=DEBUG

# Configuración de la base de datos
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=sigopel2
spring.datasource.password=123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Configuración JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=sigopel2

# Logs SQL y Web
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.root=INFO
logging.level.com.tuapp=DEBUG

# Archivos de log (simple)
logging.file.name=logs/app.log

# Niveles de log más detallados
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework=INFO
logging.level.org.hibernate=INFO

# Mostrar el "reason" de ResponseStatusException en el body
server.error.include-message=always
server.error.include-binding-errors=never
server.error.include-exception=false

spring.web.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

